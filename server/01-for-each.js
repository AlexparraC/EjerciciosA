const letters = [ 'a', 'b', 'c'];

for (let index = 0; index < letters.length; index++) {
    let element = letters[index];
    console.log(element);
    }
    const letters1 = [ 'a', 'b', 'c'];

    letters1.forEach(item => console.log(item));
    
console.log("hello world"); 
